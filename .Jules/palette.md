## 2025-05-15 - Range Input Null State
**Learning:** Native HTML range inputs cannot represent a `null` state (they always have a value). When used for optional numeric values like personal ratings, users need a way to unset the value.
**Action:** Provide a separate "Clear" or "Reset" button next to range inputs to allow users to return the value to `null`.
